resolver: lts-7.10

packages:
- .
- jni
- jvm

extra-deps:
- thread-local-storage-0.1.0.3

nix:
  # Requires Stack >= 1.2.
  shell-file: shell.nix

docker:
  enable: false
  image: tweag/inline-java
  stack-exe: image
