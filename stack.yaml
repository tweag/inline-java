require-stack-version: ">= 1.6"

resolver: lts-10.1

packages:
- .
- jni
- jvm
- jvm-streaming
- examples/classpath

nix:
  shell-file: shell.nix
  path: ["nixpkgs=./nixpkgs.nix"]

docker:
  enable: false
  image: tweag/inline-java
  stack-exe: image
